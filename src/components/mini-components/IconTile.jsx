import { styles } from "./icon_title";
import { Box } from "@mui/material";
import { colors } from "../../utils/const";
import Link from "next/link";

export default function Text({ title, lineWidth }) {
  return <Box sx={styles.container}></Box>;
}
