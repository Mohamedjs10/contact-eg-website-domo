import { colors } from "../../utils/const";

export const styles = {
  sectionContainer: (theme) => ({
    display: "flex",
    justifyContent: "space-around",
    alignItems: "center",
    flexWrap: "wrap",
    gap: 4,
  }),
  container: (theme) => ({}),
};
