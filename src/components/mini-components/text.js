import { colors } from "../../utils/const";

export const styles = {
  title: (theme) => ({
    color: colors.orange,
    fontSize: "56px",
    fontWeight: "bold",
  }),
  body: (theme) => ({
    color: colors.navy_blue,
    fontSize: "16px",
    fontWeight: "bold",
  }),
};
