export const styles = {
  container: (theme) => ({
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  }),
  img: (theme) => ({
    width: "80%",
  }),
  body: (theme) => ({
    display: "flex",
    flexDirection: "column",
    // alignItems: "center",
    gap: 5,
    width: "100%",
  }),
};
